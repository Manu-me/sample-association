<%= simple_form_for([@kid, @candy]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    
    <%#= f.input :kid_id, :as => :hidden, :input_html => { :value => @kid.id }%>
   <%= f.hidden_field :kid_id, :value => @kid.id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
